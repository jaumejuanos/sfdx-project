<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Lead_Quality_Helper__c</fullName>
    <externalId>false</externalId>
    <formula>IF(ISBLANK(Email),0,1) + IF(ISBLANK(Phone),0,1) + IF(ISBLANK(Company),0,1) + IF(ISPICKVAL(Industry, &apos;&apos;),0,1) + IF(ISBLANK(Title),0,1)</formula>
    <label>Lead Quality Helper</label>
    <precision>18</precision>
    <required>false</required>
    <scale>0</scale>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
